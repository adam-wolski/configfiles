order += "arch_updates"
order += "spotify"
order += "volume_status"
order += "battery_level"
order += "time"

spotify {
    format = "{title} by {artist}"
    format_down = ""
}

battery_level {
    format = "{percent}%"
    hide_seconds = True
    notify_low_level = True
    notification = True
}

arch_updates {
    hide_if_zero = True
}
